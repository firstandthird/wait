options:
  banner: '<%= meta.banner %>'
dist:
  src: 'dist/fidel.<%= name %>.js'
  dest: 'dist/fidel.<%= name %>.min.js'
full:
  src: 'dist/<%= name %>.full.js'
  dest: 'dist/<%= name %>.full.min.js'
